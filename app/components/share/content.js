import React, {
  Component,
  StyleSheet,
  View,
  Text,
} from 'react-native';

const styles = StyleSheet.create({
});

export default class Content extends Component {
  render() {
    return (
      <View></View>
    );
  }
}
